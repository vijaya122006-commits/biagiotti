# utils/fuzzy_matcher.py

def fuzzy_match_products(product_name, database_names=[]):
    # PLACE ML / NLP MODEL FUZZY MATCHING HERE
    # Dummy implementation returns the original name with a dummy match score
    return {
        "matched_name": product_name,
        "score": 0.95
    }
